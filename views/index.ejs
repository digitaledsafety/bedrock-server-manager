<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Minecraft Bedrock Server Manager</title>
    <link rel="stylesheet" href="/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1>Minecraft Bedrock Server Manager</h1>

        <div id="messageBox" style="display: none;"></div>

        <div class="section">
            <div class="section-header">
                <h3>Server Control</h3>
                <div>
                    Server Status: <span id="serverStatus" class="status-<%= serverStatus %>"><%= serverStatus.toUpperCase() %></span>
                </div>
            </div>
            <div class="button-group">
                <button id="startButton">Start Server</button>
                <button id="stopButton">Stop Server</button>
                <button id="restartButton">Restart Server</button>
                <button id="updateButton">Check & Install Update</button>
            </div>
        </div>

        <div class="section">
            <h3>Server Properties</h3>
            <form id="propertiesForm" class="form-grid">
                <% for (const key in properties) { %>
                    <div class="form-group">
                        <label for="<%= key %>"><%= key %>:</label>
                        <input type="text" id="<%= key %>" name="<%= key %>" value="<%= properties[key] %>">
                    </div>
                <% } %>
                <div class="form-group">
                    <button type="submit">Save Properties</button>
                </div>
            </form>
        </div>

        <div class="section">
            <h3>World Management</h3>
            <div class="world-select-container">
                <label for="worldSelect">Active World:</label>
                <select id="worldSelect">
                    <option value="">Loading worlds...</option>
                    <% worlds.forEach(world => { %>
                        <option value="<%= world %>" <%= properties['level-name'] === world ? 'selected' : '' %>><%= world %></option>
                    <% }); %>
                </select>
                <button id="activateWorldButton">Activate Selected World</button>
            </div>
            <p>To create a new world, simply change the "level-name" in Server Properties to a new name and restart the server. A new world will be generated.</p>
        </div>
    </div>

    <script src="/script.js"></script>
</body>
</html>